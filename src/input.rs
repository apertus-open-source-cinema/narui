/* Input Handling:
Hit testing for the onClick handler is only done on MouseDown.
The component with the highest z-index, that is sensitive to Input on that location will be notified
that event and of subsequent events until the mouse click is released.
 */
